name: Deploy to GitHub Pages

on: workflow_dispatch

env:
  HATCH_VERSION: "1.7.0"
  PYTHON_VERSION: "3.9"
  JUPYTERLAB_DATALOGGER_VERSION: "0.3.1"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Hatch
        run: pipx install hatch==${{ env.HATCH_VERSION }}

      - name: Set up Python with pip cache
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc

      - name: Yarn cache
        uses: actions/cache@v3
        with:
          path: .yarn/cache
          key: yarn-${{ runner.os }}-${{ hashFiles('yarn.lock') }}
          restore-keys: |
            yarn-${{ runner.os }}-

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: yarn

      - name: Build lab extension
        run: yarn build

      - name: Build package
        run: hatch build

      - name: Create _site/releases/index.html
        run: |
          mkdir _site
          mkdir _site/releases
          echo '<!DOCTYPE html>
          <html>
            <body>
              <a href="jupyterlab-datalogger/">jupyterlab-datalogger</a>
            </body>
          </html>' > _site/releases/index.html

      - name: Create _site/releases/jupyterlab-datalogger/index.html
        run: |
          mkdir _site/releases/jupyterlab-datalogger
          echo '<!DOCTYPE html>
          <html>
            <body>
              <a href="jupyterlab_datalogger-${{ env.JUPYTERLAB_DATALOGGER_VERSION }}-py3-none-any.whl">
                jupyterlab_datalogger-${{ env.JUPYTERLAB_DATALOGGER_VERSION }}-py3-none-any.whl
              </a>
              <a href="jupyterlab_datalogger-${{ env.JUPYTERLAB_DATALOGGER_VERSION }}.tar.gz">jupyterlab_datalogger-${{ env.JUPYTERLAB_DATALOGGER_VERSION }}.tar.gz</a>
            </body>
          </html>' > _site/releases/jupyterlab-datalogger/index.html

      - name: Move package files into _site/releases/jupyterlab-datalogger
        run: mv dist/* _site/releases/jupyterlab-datalogger

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v2

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
